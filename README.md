# Sistema de Préstamos

Proyecto completo con **Laravel (backend)** y **Flutter (frontend)**.

## 🚀 Backend (Laravel)
- Requisitos: PHP, Composer, MySQL
- Instalación:
  ```bash
  cd backend
  composer install
  cp .env.example .env
  php artisan key:generate
  php artisan migrate --seed
  php artisan serve

cd frontend
flutter pub get
flutter run


## 🚀Estructura
sistema-prestamos/
├── backend/   # Laravel
└── frontend/  # Flutter
